services:
  blog-api:
    image: node:20-alpine
    working_dir: /app
    ports:
      - 3000:3000
    volumes:
      - .:/app
    depends_on:
      - mongodb
    command: sh -c "npm install && npm run start:dev"
  mongodb:
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - mongodb-data:/data/db

volumes:
  mongodb-data:
